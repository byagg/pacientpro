/**
 * Application-wide constants
 * Centralized values to ensure consistency across the application
 */

/**
 * Handling fee per patient examination (in EUR)
 * Used for commission calculations and invoice generation
 */
export const PATIENT_FEE = 14;

/**
 * Date range for filtering historical data (in years)
 * Patients and appointments older than this are automatically hidden
 */
export const DATA_RETENTION_YEARS = 1;

